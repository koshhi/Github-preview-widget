---
phase: 06-end-to-end-widget-rendering
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widget/runtime/createOrRefreshEmbedFromUrl.ts
  - src/widget/runtime/normalizeRenderForWidget.ts
  - src/widget/runtime/persistWidgetSnapshot.ts
  - src/widget/code.ts
  - src/widget/ui.html
  - src/widget/__tests__/runtimePipeline.test.ts
  - src/widget/__tests__/renderNormalization.test.ts
  - src/widget/__tests__/widgetStatePersistence.test.ts
  - src/widget/code.js
autonomous: true
requirements:
  - WDG-03
  - INT-04
  - INT-05
user_setup: []
must_haves:
  truths:
    - "Una URL válida dispara pipeline real: read GitHub -> render -> actualización in-place del embed de la misma instancia."
    - "El widget muestra estructura `header/body/footer` con metadata visible y estado sync actualizado."
    - "Markdown renderiza bloques base; Mermaid válido se muestra como code-view en fase 6 y Mermaid inválido cae en fallback con warning."
    - "En fallo de fetch/render se conserva el último contenido válido y se actualiza `syncState=error` con detalle visible."
    - "El runtime mantiene objetivo de primer resultado útil rápido con política progresiva para contenido grande."
  artifacts:
    - path: src/widget/runtime/createOrRefreshEmbedFromUrl.ts
      provides: Orquestación end-to-end URL->GitHub->render->embed update
      exports: ["createOrRefreshEmbedFromUrl"]
    - path: src/widget/runtime/normalizeRenderForWidget.ts
      provides: Adaptación de bloques render para política UX fase 6 (Mermaid code-view)
      exports: ["normalizeRenderForWidget"]
    - path: src/widget/runtime/persistWidgetSnapshot.ts
      provides: Persistencia de snapshot mínimo en synced state
      exports: ["buildWidgetSnapshot", "mergeWidgetSnapshot"]
    - path: src/widget/code.ts
      provides: Integración UI/runtime con create/update in-place y errores inline
      exports: ["GitHubPreviewWidget"]
    - path: src/widget/ui.html
      provides: UX de submit + error inline + estado de loading + confirmación de creación
      exports: ["ui-create-preview-flow"]
  key_links:
    - from: src/widget/code.ts
      to: src/widget/runtime/createOrRefreshEmbedFromUrl.ts
      via: handler de `create-preview` delega pipeline completo
      pattern: "createOrRefreshEmbedFromUrl\("
    - from: src/widget/runtime/createOrRefreshEmbedFromUrl.ts
      to: src/core/github/readGithubFileWithAuth.ts
      via: lectura de contenido remoto con auth flow existente
      pattern: "readGithubFileWithAuth\("
    - from: src/widget/runtime/createOrRefreshEmbedFromUrl.ts
      to: src/core/render/renderFilePreview.ts
      via: render principal y modo progresivo
      pattern: "renderFilePreview\("
    - from: src/widget/runtime/createOrRefreshEmbedFromUrl.ts
      to: src/widget/runtime/normalizeRenderForWidget.ts
      via: adaptación de Mermaid/warnings para widget UX
      pattern: "normalizeRenderForWidget\("
    - from: src/widget/runtime/createOrRefreshEmbedFromUrl.ts
      to: src/core/canvas/updateEmbedBlock.ts
      via: actualización in-place sobre embed existente
      pattern: "updateEmbedBlockInPlace\("
---

<objective>
Implementar el flujo end-to-end real del widget para convertir URL de GitHub en embed actualizado in-place con render y metadata visibles.

Purpose: Cumplir WDG-03/INT-04/INT-05 con pipeline real y comportamiento UX definido en contexto.
Output: Runtime orchestrator + normalización de render + persistencia de snapshot + integración UI/runtime + tests de integración.
</objective>

<execution_context>
@/Users/cesar/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cesar/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-end-to-end-widget-rendering/06-CONTEXT.md
@.planning/phases/06-end-to-end-widget-rendering/06-RESEARCH.md
@src/core/github/readGithubFileWithAuth.ts
@src/core/render/renderFilePreview.ts
@src/core/canvas/updateEmbedBlock.ts
@src/widget/code.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Construir orquestador runtime URL -> render -> embed in-place</name>
  <files>src/widget/runtime/createOrRefreshEmbedFromUrl.ts, src/widget/runtime/persistWidgetSnapshot.ts, src/widget/__tests__/runtimePipeline.test.ts</files>
  <action>Crear un orquestador puro que reciba URL + estado actual de instancia y encadene `readGithubFileWithAuth`, `renderFilePreview` y `updateEmbedBlockInPlace`. Implementar persistencia de snapshot mínimo (`sourceKey`, `sourceUrl`, `syncState`, `lastSync`, `warnings`, resumen render) y política de conservación de último contenido válido ante error. Añadir tests de pipeline nominal, error fetch y error render.</action>
  <verify>npm test -- src/widget/__tests__/runtimePipeline.test.ts && npm run typecheck</verify>
  <done>Pipeline end-to-end funcional y testeado, con update in-place y conservación de estado válido en errores (INT-04, WDG-03 parcial).</done>
</task>

<task type="auto">
  <name>Task 2: Integrar pipeline en `code.ts` y UX UI de create-preview</name>
  <files>src/widget/code.ts, src/widget/ui.html, src/widget/__tests__/widgetStatePersistence.test.ts</files>
  <action>Conectar handler `create-preview` de UI al orquestador runtime. Implementar estados inline (`Syncing...`, error inline, success) y mantener UI abierta tras éxito. Asegurar metadata visible en header/footer del embed y soporte de actualización in-place cuando cambia URL en la misma instancia. Añadir tests de persistencia/actualización de estado en create y update.</action>
  <verify>npm test -- src/widget/__tests__/widgetStatePersistence.test.ts && npm run widget:build && npm run widget:check</verify>
  <done>Widget crea/actualiza instancia real en canvas con UX esperada y metadatos visibles (WDG-03, INT-04).</done>
</task>

<task type="auto">
  <name>Task 3: Normalizar Markdown/Mermaid para política UX de fase 6</name>
  <files>src/widget/runtime/normalizeRenderForWidget.ts, src/widget/runtime/createOrRefreshEmbedFromUrl.ts, src/widget/__tests__/renderNormalization.test.ts</files>
  <action>Implementar normalizador de bloques render para fase 6: Markdown base completo, Mermaid mostrado como bloque code, fallback warning cuando Mermaid inválido, y mapeo de warnings a badge/footer. Ajustar política progresiva para primer resultado rápido (<2s nominal en condiciones definidas). Añadir tests específicos para Mermaid válido/ inválido y warnings visibles.</action>
  <verify>npm test -- src/widget/__tests__/renderNormalization.test.ts && npm run typecheck</verify>
  <done>Render en widget cumple política de Markdown/Mermaid con fallback controlado y warnings visibles (INT-05).</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- src/widget/__tests__/runtimePipeline.test.ts` pasa en verde
- [ ] `npm test -- src/widget/__tests__/widgetStatePersistence.test.ts` pasa en verde
- [ ] `npm test -- src/widget/__tests__/renderNormalization.test.ts` pasa en verde
- [ ] `npm run widget:build` pasa en verde
- [ ] `npm run widget:check` pasa en verde
- [ ] `npm run typecheck` pasa sin errores
- [ ] WDG-03, INT-04, INT-05 quedan cubiertos explícitamente por tareas, tests y evidencias de summary
</verification>

<success_criteria>

- Todas las tareas completadas
- URL válida activa pipeline real y actualiza embed in-place
- Markdown/Mermaid se muestran según decisiones de contexto con fallback visible
- Estado de error conserva contenido válido previo y deja trazabilidad de warning/sync
  </success_criteria>

<output>
After completion, create `.planning/phases/06-end-to-end-widget-rendering/06-01-SUMMARY.md`
</output>
