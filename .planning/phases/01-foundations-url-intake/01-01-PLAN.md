---
phase: 01-foundations-url-intake
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/url/types.ts
  - src/core/url/parseGithubFileUrl.ts
  - src/core/url/validateGithubFileUrl.ts
  - src/core/url/detectFileKind.ts
  - src/core/url/ingestGithubFileUrl.ts
  - src/core/url/errors.ts
  - src/core/url/__tests__/parseGithubFileUrl.test.ts
  - src/core/url/__tests__/validateGithubFileUrl.test.ts
  - src/core/url/__tests__/ingestGithubFileUrl.test.ts
autonomous: true
requirements:
  - SRC-01
  - SRC-02
  - SRC-03
must_haves:
  truths:
    - "Usuario puede pegar una URL de fichero GitHub en formato blob o raw y se obtiene un resultado normalizado."
    - "URL inválida o no soportada devuelve error accionable con código estable."
    - "La extensión del fichero se detecta correctamente para md, txt, json, js y ts."
  artifacts:
    - path: src/core/url/parseGithubFileUrl.ts
      provides: Parser de URL GitHub blob/raw hacia owner/repo/ref/path
      exports: ["parseGithubFileUrl"]
    - path: src/core/url/validateGithubFileUrl.ts
      provides: Reglas de contrato y validación de fichero
      exports: ["validateGithubFileUrl"]
    - path: src/core/url/detectFileKind.ts
      provides: Detección de extensión soportada para routing inicial
      exports: ["detectFileKind"]
    - path: src/core/url/ingestGithubFileUrl.ts
      provides: Orquestación parse + validate + detect para uso del plugin
      exports: ["ingestGithubFileUrl"]
  key_links:
    - from: src/core/url/parseGithubFileUrl.ts
      to: src/core/url/validateGithubFileUrl.ts
      via: Resultado normalizado se valida antes de devolver éxito
      pattern: "validateGithubFileUrl\\("
    - from: src/core/url/validateGithubFileUrl.ts
      to: src/core/url/detectFileKind.ts
      via: Validación de fichero consume detector de extensión soportada
      pattern: "detectFileKind\\("
    - from: src/core/url/ingestGithubFileUrl.ts
      to: src/core/url/errors.ts
      via: Errores internos se traducen a formato accionable estable
      pattern: "UrlValidationErrorCode|toUserFacingUrlError"
---

<objective>
Implementar la base de ingestión de URLs GitHub para la Fase 1, cubriendo parseo, normalización, validación y detección de tipo de fichero.

Purpose: Reducir ambigüedad al inicio del flujo para que fases posteriores (auth/render/sync) consuman un contrato estable.
Output: Módulos de URL contract + tests de comportamiento para SRC-01/SRC-02/SRC-03.
</objective>

<execution_context>
@/Users/cesar/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cesar/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-url-intake/01-CONTEXT.md
@.planning/phases/01-foundations-url-intake/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Definir contrato tipado y parser blob/raw</name>
  <files>src/core/url/types.ts, src/core/url/parseGithubFileUrl.ts, src/core/url/__tests__/parseGithubFileUrl.test.ts</files>
  <action>Crear tipos de dominio para URL normalizada y códigos de error. Implementar `parseGithubFileUrl` soportando exactamente `github.com/.../blob/...` y `raw.githubusercontent.com/...`, extrayendo `owner`, `repo`, `ref`, `path` sin ambigüedad. Rechazar rutas no fichero y casos no soportados. Añadir tests con casos válidos e inválidos, incluyendo ramas complejas y rutas profundas.</action>
  <verify>npm test -- src/core/url/__tests__/parseGithubFileUrl.test.ts</verify>
  <done>Parser devuelve estructura normalizada para URLs válidas y error tipado para rutas inválidas/no soportadas.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar validación de contrato y detección de extensión</name>
  <files>src/core/url/validateGithubFileUrl.ts, src/core/url/detectFileKind.ts, src/core/url/errors.ts, src/core/url/__tests__/validateGithubFileUrl.test.ts</files>
  <action>Implementar `validateGithubFileUrl` para aplicar reglas del contrato de fase (solo fichero, host/ruta soportados). Implementar `detectFileKind` con allowlist para `.md`, `.txt`, `.json`, `.js`, `.ts`, devolviendo error estable para extensión no soportada. Añadir mapeo de error interno a mensaje accionable de UI sin filtrar detalles técnicos innecesarios.</action>
  <verify>npm test -- src/core/url/__tests__/validateGithubFileUrl.test.ts</verify>
  <done>Validador y detector cubren SRC-02/SRC-03 con errores estables y copy accionable.</done>
</task>

<task type="auto">
  <name>Task 3: Unificar ingestión y endurecer pruebas de regresión</name>
  <files>src/core/url/ingestGithubFileUrl.ts, src/core/url/__tests__/ingestGithubFileUrl.test.ts</files>
  <action>Crear `ingestGithubFileUrl` como entrypoint puro que orquesta parseo, validación y detección. Asegurar que la salida sirve a fases siguientes (fetch/render) sin reparseos adicionales. Añadir suite de regresión con matriz de casos: blob válido, raw válido, URL de carpeta, URL repo raíz, extensión no soportada y errores de formato.</action>
  <verify>npm test -- src/core/url/__tests__/ingestGithubFileUrl.test.ts && npm run typecheck</verify>
  <done>Existe un único punto de entrada de ingestión con comportamiento determinista y cobertura de casos críticos del contrato de fase.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- src/core/url/__tests__/` pasa en verde
- [ ] `npm run typecheck` pasa sin errores
- [ ] Cada requisito SRC-01/SRC-02/SRC-03 tiene al menos un test explícito asociado
</verification>

<success_criteria>

- Todas las tareas completadas
- Validación y parseo alineados con decisiones bloqueadas en CONTEXT.md
- Errores de URL no soportada son claros y accionables
- Detección de extensiones v1 mínima implementada y testeada
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-url-intake/01-01-SUMMARY.md`
</output>
