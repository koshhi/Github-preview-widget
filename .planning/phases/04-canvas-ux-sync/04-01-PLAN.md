---
phase: 04-canvas-ux-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/canvas/types.ts
  - src/core/canvas/embedBlockModel.ts
  - src/core/canvas/composeEmbedBlock.ts
  - src/core/canvas/updateEmbedBlock.ts
  - src/core/canvas/syncState.ts
  - src/core/canvas/refreshManual.ts
  - src/core/canvas/refreshAuto.ts
  - src/core/canvas/__tests__/composeEmbedBlock.test.ts
  - src/core/canvas/__tests__/syncState.test.ts
  - src/core/canvas/__tests__/refreshManual.test.ts
  - src/core/canvas/__tests__/refreshAuto.test.ts
autonomous: true
requirements:
  - CVS-01
  - CVS-02
  - SNC-01
  - SNC-02
  - SNC-03
user_setup: []
must_haves:
  truths:
    - "El bloque embebido se compone como `header + body + footer` con estilo compacto y actualización in-place."
    - "Header muestra siempre `owner/repo`, `path` y `último sync`, junto a estado semántico visible."
    - "Refresh manual se puede disparar desde header y menú contextual, mostrando `Syncing...` y preservando contenido previo en error."
    - "Auto-refresh se ejecuta en apertura/reentrada solo para bloques elegibles y registra estado `Auto-sync` con timestamp."
    - "Resultado de sync (éxito/error) queda visible con mensaje corto y detalle técnico expandible."
  artifacts:
    - path: src/core/canvas/composeEmbedBlock.ts
      provides: Composición de bloque canvas nativo con header/body/footer y metadatos mínimos
      exports: ["composeEmbedBlock"]
    - path: src/core/canvas/updateEmbedBlock.ts
      provides: Actualización in-place del bloque sin recrear nodo
      exports: ["updateEmbedBlockInPlace"]
    - path: src/core/canvas/refreshManual.ts
      provides: Flujo refresh manual con triggers header/context menu y estado syncing
      exports: ["refreshBlockManual"]
    - path: src/core/canvas/refreshAuto.ts
      provides: Trigger de auto-refresh por apertura/reentrada con elegibilidad
      exports: ["refreshEligibleBlocksOnOpen"]
    - path: src/core/canvas/syncState.ts
      provides: Máquina de estados sync (`idle/syncing/success/error`) con modo manual/auto
      exports: ["transitionSyncState", "buildSyncBadge"]
  key_links:
    - from: src/core/canvas/refreshManual.ts
      to: src/core/canvas/syncState.ts
      via: Refresh manual actualiza estado y badge en cada transición
      pattern: "transitionSyncState\\("
    - from: src/core/canvas/refreshAuto.ts
      to: src/core/canvas/syncState.ts
      via: Auto-sync marca modo `auto` y timestamp visible
      pattern: "mode:\\s*\"auto\"|transitionSyncState\\("
    - from: src/core/canvas/updateEmbedBlock.ts
      to: src/core/canvas/composeEmbedBlock.ts
      via: Estructura visual se preserva y solo se actualizan secciones necesarias
      pattern: "composeEmbedBlock\\(|header|footer|body"
    - from: src/core/canvas/refreshManual.ts
      to: src/core/render/renderFilePreview.ts
      via: Refresh consume render pipeline de fase 3 para actualizar body
      pattern: "renderFilePreview\\("
---

<objective>
Implementar la capa de Canvas UX & Sync para que el embed se vea nativo, muestre estado claro y soporte refresh manual/auto sin recreación de nodos.

Purpose: Cumplir CVS-01/CVS-02/SNC-01/SNC-02/SNC-03 con un flujo estable y observable.
Output: Modelo/compositor de bloque + máquina de estado de sync + handlers manual/auto + tests de comportamiento.
</objective>

<execution_context>
@/Users/cesar/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cesar/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-canvas-ux-sync/04-CONTEXT.md
@.planning/phases/04-canvas-ux-sync/04-RESEARCH.md
@.planning/phases/03-render-engine/03-01-SUMMARY.md
@src/core/render/renderFilePreview.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modelar y componer bloque embebido nativo en canvas</name>
  <files>src/core/canvas/types.ts, src/core/canvas/embedBlockModel.ts, src/core/canvas/composeEmbedBlock.ts, src/core/canvas/__tests__/composeEmbedBlock.test.ts</files>
  <action>Definir contrato del bloque con secciones `header/body/footer`, metadatos mínimos y propiedades de tamaño/redimensionado (ancho configurable, alto flexible). Implementar `composeEmbedBlock` con layout compacto, estilo neutral y metadata visible en header. Añadir tests que validen estructura, campos obligatorios (`owner/repo`, `path`, `lastSync`) y soporte de resize por borde.</action>
  <verify>npm test -- src/core/canvas/__tests__/composeEmbedBlock.test.ts</verify>
  <done>Bloque nativo de canvas queda compuesto con estructura estable y metadatos mínimos visibles (CVS-01/CVS-02).</done>
</task>

<task type="auto">
  <name>Task 2: Implementar estado de sync y refresh manual in-place</name>
  <files>src/core/canvas/syncState.ts, src/core/canvas/updateEmbedBlock.ts, src/core/canvas/refreshManual.ts, src/core/canvas/__tests__/syncState.test.ts, src/core/canvas/__tests__/refreshManual.test.ts</files>
  <action>Implementar máquina de estados `idle/syncing/success/error` con modo manual/auto, mensaje corto y detalle expandible. Implementar `refreshBlockManual` con triggers `header` y `context_menu`, mostrando `Syncing...` + spinner discreto y preservando contenido previo si falla. Implementar actualización in-place (`updateEmbedBlockInPlace`) para evitar recrear el nodo. Añadir tests de transiciones y path éxito/error.</action>
  <verify>npm test -- src/core/canvas/__tests__/syncState.test.ts && npm test -- src/core/canvas/__tests__/refreshManual.test.ts</verify>
  <done>Refresh manual cumple SNC-01/SNC-03 con feedback visible y sin recreación de bloque.</done>
</task>

<task type="auto">
  <name>Task 3: Implementar auto-refresh por apertura/reentrada con elegibilidad</name>
  <files>src/core/canvas/refreshAuto.ts, src/core/canvas/__tests__/refreshAuto.test.ts</files>
  <action>Implementar `refreshEligibleBlocksOnOpen` para disparar auto-sync solo en bloques con `sourceKey` válido y estado no bloqueante. Integrar resultado de render con actualización in-place y registrar estado como `Auto-sync` con timestamp. Garantizar que fallos no bloquean UI y mantienen contenido previo con estado de error visible. Añadir tests para filtro de elegibilidad, éxito, error no bloqueante y marcación de modo `auto`.</action>
  <verify>npm test -- src/core/canvas/__tests__/refreshAuto.test.ts && npm run typecheck</verify>
  <done>Auto-refresh cumple SNC-02/SNC-03 con reglas de elegibilidad y trazabilidad de modo.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- src/core/canvas/__tests__/composeEmbedBlock.test.ts` pasa en verde
- [ ] `npm test -- src/core/canvas/__tests__/syncState.test.ts` pasa en verde
- [ ] `npm test -- src/core/canvas/__tests__/refreshManual.test.ts` pasa en verde
- [ ] `npm test -- src/core/canvas/__tests__/refreshAuto.test.ts` pasa en verde
- [ ] `npm run typecheck` pasa sin errores
- [ ] Cada requisito CVS-01/CVS-02/SNC-01/SNC-02/SNC-03 tiene al menos un test explícito asociado
</verification>

<success_criteria>

- Todas las tareas completadas
- Bloque canvas mantiene apariencia nativa y metadatos mínimos visibles
- Refresh manual y auto actualizan contenido in-place sin recrear nodo
- Estado de sincronización comunica claramente éxito/error con timestamp y detalle
  </success_criteria>

<output>
After completion, create `.planning/phases/04-canvas-ux-sync/04-01-SUMMARY.md`
</output>
