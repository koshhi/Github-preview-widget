---
phase: 02-private-access-with-pat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/auth/types.ts
  - src/core/auth/patStore.ts
  - src/core/auth/__tests__/patStore.test.ts
  - src/core/github/types.ts
  - src/core/github/classifyAuthError.ts
  - src/core/github/authUxMessages.ts
  - src/core/github/fetchGithubFile.ts
  - src/core/github/readGithubFileWithAuth.ts
  - src/core/github/__tests__/classifyAuthError.test.ts
  - src/core/github/__tests__/readGithubFileWithAuth.test.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
user_setup: []
must_haves:
  truths:
    - "El plugin solo solicita PAT cuando el acceso al fichero falla por privado y no existe token válido para ese sourceKey."
    - "Un PAT válido por fichero permite leer el contenido privado sin exponer credenciales en canvas."
    - "Errores de auth se clasifican y muestran con copy explícito para missing, expired/invalid y insufficient scope."
    - "Cuando un PAT falla se conserva en store como invalid y el flujo aplica exactamente un reintento automático antes del error final."
  artifacts:
    - path: src/core/auth/patStore.ts
      provides: Store per-file de PAT con operaciones set/get/remove y estado valid/invalid/unknown
      exports: ["createPatStore"]
    - path: src/core/github/classifyAuthError.ts
      provides: Clasificador de errores de auth para distinguir missing_pat, expired_pat y current_pat
      exports: ["classifyAuthError"]
    - path: src/core/github/authUxMessages.ts
      provides: Mapeo estable de copy UX para escenarios de auth definidos en CONTEXT
      exports: ["getAuthUxMessage"]
    - path: src/core/github/readGithubFileWithAuth.ts
      provides: Orquestación de lectura público->privado con PAT por fichero y un único reintento automático
      exports: ["readGithubFileWithAuth"]
  key_links:
    - from: src/core/github/readGithubFileWithAuth.ts
      to: src/core/auth/patStore.ts
      via: Source key normalizado consulta/actualiza estado del PAT por fichero
      pattern: "patStore\\.(get|set|markInvalid|remove)"
    - from: src/core/github/readGithubFileWithAuth.ts
      to: src/core/github/classifyAuthError.ts
      via: Respuestas HTTP/GitHub se traducen a códigos auth antes de decidir retry
      pattern: "classifyAuthError\\("
    - from: src/core/github/readGithubFileWithAuth.ts
      to: src/core/github/authUxMessages.ts
      via: Resultado auth se convierte en mensaje UX final definido por fase
      pattern: "getAuthUxMessage\\("
    - from: src/core/github/readGithubFileWithAuth.ts
      to: src/core/url/ingestGithubFileUrl.ts
      via: Entrada usa contrato de fase 1 para construir sourceKey estable
      pattern: "ingestGithubFileUrl\\("
---

<objective>
Implementar la capa de acceso privado con PAT por fichero para lectura de archivos GitHub, con reintento controlado y UX de error accionable.

Purpose: Cumplir el contrato AUTH-01/02/03 sin romper el flujo público ni mezclar auth con parseo de URL.
Output: Módulos de PAT store + fetch/orquestación auth + clasificación/mensajería + tests de flujo privado.
</objective>

<execution_context>
@/Users/cesar/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cesar/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-private-access-with-pat/02-CONTEXT.md
@.planning/phases/02-private-access-with-pat/02-RESEARCH.md
@.planning/phases/01-foundations-url-intake/01-01-SUMMARY.md
@src/core/url/ingestGithubFileUrl.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Definir modelo y store PAT por fichero</name>
  <files>src/core/auth/types.ts, src/core/auth/patStore.ts, src/core/auth/__tests__/patStore.test.ts</files>
  <action>Crear tipos de dominio para credencial por `sourceKey` con estado `unknown|valid|invalid`, `lastValidatedAt` y `lastErrorCode`. Implementar `createPatStore` con operaciones puras para set/get/remove/markInvalid, sin persistir en canvas y sin concepto de token global. Añadir pruebas que cubran reemplazo manual del PAT, borrado explícito y conservación de token marcado inválido.</action>
  <verify>npm test -- src/core/auth/__tests__/patStore.test.ts</verify>
  <done>Existe store per-file determinista con API mínima para AUTH-01 y cobertura de reemplazo/borrado/invalidación.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar fetch GitHub y clasificación de errores auth</name>
  <files>src/core/github/types.ts, src/core/github/fetchGithubFile.ts, src/core/github/classifyAuthError.ts, src/core/github/authUxMessages.ts, src/core/github/__tests__/classifyAuthError.test.ts</files>
  <action>Implementar cliente GitHub con cabecera `Authorization` condicional y resultado tipado (status/body/headers relevantes). Implementar `classifyAuthError` para distinguir `missing_pat`, `expired_pat`, `current_pat` y no-auth. Implementar `getAuthUxMessage` con copy exactamente acordado en CONTEXT.md para los tres escenarios de auth, manteniendo códigos estables separados del texto de UI. Añadir pruebas unitarias para matriz de respuestas 401/403/404 y mapping de mensajes.</action>
  <verify>npm test -- src/core/github/__tests__/classifyAuthError.test.ts</verify>
  <done>Clasificación de auth y copy UX están desacoplados del HTTP crudo y cubren AUTH-03 con tests.</done>
</task>

<task type="auto">
  <name>Task 3: Orquestar lectura público/privado con un reintento automático</name>
  <files>src/core/github/readGithubFileWithAuth.ts, src/core/github/__tests__/readGithubFileWithAuth.test.ts</files>
  <action>Crear `readGithubFileWithAuth` que use `ingestGithubFileUrl` (fase 1) para construir `sourceKey` y ejecute flujo: intento sin token, detección de privado, uso automático de PAT guardado si existe, marcado `invalid` cuando falla y un único reintento automático antes de devolver error final. Asegurar que solo se solicita PAT cuando aplica (fichero privado) y que el resultado incluye estado consumible por UI para refresh manual/auto posterior.</action>
  <verify>npm test -- src/core/github/__tests__/readGithubFileWithAuth.test.ts && npm run typecheck</verify>
  <done>Flujo público/privado queda unificado con retry=1, persistencia de estado inválido y salida estable para AUTH-02.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- src/core/auth/__tests__/patStore.test.ts` pasa en verde
- [ ] `npm test -- src/core/github/__tests__/classifyAuthError.test.ts` pasa en verde
- [ ] `npm test -- src/core/github/__tests__/readGithubFileWithAuth.test.ts` pasa en verde
- [ ] `npm run typecheck` pasa sin errores
- [ ] Cada requisito AUTH-01/AUTH-02/AUTH-03 tiene al menos un test explícito asociado
</verification>

<success_criteria>

- Todas las tareas completadas
- PAT por fichero implementado sin exponer credenciales en canvas
- Flujo público->privado con un solo reintento automático validado por tests
- Mensajes de auth alineados con copy bloqueado en CONTEXT.md
  </success_criteria>

<output>
After completion, create `.planning/phases/02-private-access-with-pat/02-01-SUMMARY.md`
</output>
