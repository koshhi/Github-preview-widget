---
phase: 03-render-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/render/types.ts
  - src/core/render/detectMinifiedJson.ts
  - src/core/render/highlightCode.ts
  - src/core/render/renderMarkdown.ts
  - src/core/render/renderMermaidBlocks.ts
  - src/core/render/performancePolicy.ts
  - src/core/render/renderFilePreview.ts
  - src/core/render/__tests__/highlightCode.test.ts
  - src/core/render/__tests__/renderMarkdown.test.ts
  - src/core/render/__tests__/renderMermaidBlocks.test.ts
  - src/core/render/__tests__/renderFilePreview.test.ts
autonomous: true
requirements:
  - RND-01
  - RND-02
  - RND-03
  - RND-04
user_setup: []
must_haves:
  truths:
    - "Archivos `.js/.ts/.json/.txt` se renderizan con formato legible y resaltado básico por extensión."
    - "Markdown v1 renderiza títulos, listas, enlaces, tablas y bloques de código; HTML embebido se muestra como texto."
    - "Bloques Mermaid válidos ofrecen vista de diagrama con toggle código/diagrama; errores Mermaid hacen fallback por bloque sin romper el resto."
    - "El motor aplica política de rendimiento para first preview <2s, incluyendo límite de render completo y modo progresivo para entradas grandes."
  artifacts:
    - path: src/core/render/highlightCode.ts
      provides: Render base monoespaciado con resaltado básico y pretty-print JSON minificado
      exports: ["highlightCode"]
    - path: src/core/render/renderMarkdown.ts
      provides: Render markdown subset v1 con sanitización HTML y degradación elegante
      exports: ["renderMarkdown"]
    - path: src/core/render/renderMermaidBlocks.ts
      provides: Detección/render Mermaid por bloque con fallback no bloqueante
      exports: ["renderMermaidBlocks"]
    - path: src/core/render/renderFilePreview.ts
      provides: Orquestación por tipo y política de rendimiento (<2s, truncado/progresivo)
      exports: ["renderFilePreview"]
  key_links:
    - from: src/core/render/highlightCode.ts
      to: src/core/render/detectMinifiedJson.ts
      via: Pretty-print JSON usa heurística de minificación antes de transformar
      pattern: "detectMinifiedJson\\("
    - from: src/core/render/renderMarkdown.ts
      to: src/core/render/renderMermaidBlocks.ts
      via: Markdown delega bloques Mermaid al renderer especializado
      pattern: "renderMermaidBlocks\\("
    - from: src/core/render/renderFilePreview.ts
      to: src/core/render/performancePolicy.ts
      via: Selección de render completo/progresivo depende de política de tamaño/tiempo
      pattern: "resolvePerformancePolicy\\("
    - from: src/core/render/renderFilePreview.ts
      to: src/core/render/highlightCode.ts
      via: Flujo de código/texto usa renderer básico por extensión
      pattern: "highlightCode\\("
---

<objective>
Implementar el motor de render de fase 3 para código/texto, Markdown y Mermaid con degradaciones controladas y presupuesto de rendimiento para primer preview.

Purpose: Cumplir RND-01/02/03/04 con una capa render pura y testeable, desacoplada del canvas.
Output: Pipeline `renderFilePreview` + subrenderers por tipo + política de rendimiento y tests de regresión.
</objective>

<execution_context>
@/Users/cesar/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cesar/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-render-engine/03-CONTEXT.md
@.planning/phases/03-render-engine/03-RESEARCH.md
@.planning/phases/02-private-access-with-pat/02-01-SUMMARY.md
@src/core/github/readGithubFileWithAuth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Construir renderer base para código/texto y heurística JSON</name>
  <files>src/core/render/types.ts, src/core/render/detectMinifiedJson.ts, src/core/render/highlightCode.ts, src/core/render/__tests__/highlightCode.test.ts</files>
  <action>Definir tipos de resultado de render (`blocks`, `warnings`, `truncated`, `progressive`, `metrics`). Implementar `highlightCode` para `.js/.ts/.json/.txt` con layout textual común y resaltado básico por tokens. Añadir `detectMinifiedJson` para decidir pretty-print automático de JSON minificado dentro de umbral razonable. Incluir fallback a texto plano con aviso suave para contenido corrupto o parse fallido.</action>
  <verify>npm test -- src/core/render/__tests__/highlightCode.test.ts</verify>
  <done>Renderer base cubre RND-01 con salida estable por extensión y tests para JSON minificado/fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Implementar renderer Markdown v1 con Mermaid aislado por bloque</name>
  <files>src/core/render/renderMarkdown.ts, src/core/render/renderMermaidBlocks.ts, src/core/render/__tests__/renderMarkdown.test.ts, src/core/render/__tests__/renderMermaidBlocks.test.ts</files>
  <action>Implementar `renderMarkdown` para subset v1 obligatorio (títulos, párrafos, listas, enlaces, tablas, bloques de código), tratando HTML embebido como texto. Implementar `renderMermaidBlocks` para: toggle código/diagrama cuando parsea, fallback con mensaje claro cuando falla y render parcial si solo algunos bloques fallan. Asegurar que fallos Mermaid no interrumpan el resto del markdown.</action>
  <verify>npm test -- src/core/render/__tests__/renderMarkdown.test.ts && npm test -- src/core/render/__tests__/renderMermaidBlocks.test.ts</verify>
  <done>Markdown + Mermaid cubren RND-02/RND-03 con degradación elegante y comportamiento por bloque validado.</done>
</task>

<task type="auto">
  <name>Task 3: Orquestar pipeline final con política de rendimiento y cache de preview</name>
  <files>src/core/render/performancePolicy.ts, src/core/render/renderFilePreview.ts, src/core/render/__tests__/renderFilePreview.test.ts</files>
  <action>Implementar `resolvePerformancePolicy` con límite v1 ~300 KB y decisión render completo/progresivo orientada a first preview <2s. Implementar `renderFilePreview` como orquestador por tipo consumiendo output de fase 2 y devolviendo modelo uniforme para UI/canvas. Añadir cache en memoria por `sourceKey` para refrescos sucesivos y pruebas de ruta rápida/caso truncado. Validar métricas y banderas `truncated`/`progressive` en salida.</action>
  <verify>npm test -- src/core/render/__tests__/renderFilePreview.test.ts && npm run typecheck</verify>
  <done>Pipeline final cumple RND-04 sin romper RND-01/02/03 y devuelve contrato listo para fase 4.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- src/core/render/__tests__/highlightCode.test.ts` pasa en verde
- [ ] `npm test -- src/core/render/__tests__/renderMarkdown.test.ts` pasa en verde
- [ ] `npm test -- src/core/render/__tests__/renderMermaidBlocks.test.ts` pasa en verde
- [ ] `npm test -- src/core/render/__tests__/renderFilePreview.test.ts` pasa en verde
- [ ] `npm run typecheck` pasa sin errores
- [ ] Cada requisito RND-01/RND-02/RND-03/RND-04 tiene al menos un test explícito asociado
</verification>

<success_criteria>

- Todas las tareas completadas
- Render de código/texto/markdown/mermaid alineado con decisiones de CONTEXT.md
- Fallbacks controlados implementados sin bloquear preview
- Política de rendimiento <2s aplicada con modo progresivo para entradas grandes
  </success_criteria>

<output>
After completion, create `.planning/phases/03-render-engine/03-01-SUMMARY.md`
</output>
