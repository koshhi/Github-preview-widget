---
phase: 07-private-access-pat-safety
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/widget/runtime/patSessionStore.ts
  - src/widget/runtime/redactSensitive.ts
  - src/widget/runtime/createOrRefreshEmbedFromUrl.ts
  - src/widget/bridge/messages.ts
  - src/widget/bridge/parseUiCommand.ts
  - src/widget/code.ts
  - src/widget/ui.html
  - src/widget/__tests__/patAccessFlow.test.ts
  - src/widget/__tests__/securityRedaction.test.ts
  - src/widget/code.js
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03
user_setup: []
must_haves:
  truths:
    - "Cuando el fichero es privado y no hay PAT, el widget muestra advertencia inline y permite introducir PAT por fichero sin romper el flujo."
    - "PAT válido desbloquea lectura/render de la misma instancia tras reintento manual, sin reiniciar el widget."
    - "PAT inválido/expirado y PAT sin scope suficiente se diferencian con mensajes específicos y estado inválido persistente."
    - "El PAT no aparece en canvas, metadata de embed, snapshot sincronizado ni eventos visuales UI/runtime."
  artifacts:
    - path: src/widget/runtime/patSessionStore.ts
      provides: Persistencia y ciclo de vida de PAT por `sourceKey` en sesión + clientStorage cifrado
      exports: ["createPatSessionStore", "loadPatSessionStore"]
    - path: src/widget/runtime/redactSensitive.ts
      provides: Sanitización de secretos en mensajes y detalles antes de exponerlos
      exports: ["redactSensitive"]
    - path: src/widget/code.ts
      provides: Flujo UI/runtime para auth-needed, set/forget PAT y reintento manual
      exports: ["GitHubPreviewWidget"]
    - path: src/widget/ui.html
      provides: UX inline de PAT (prompt, submit, forget, retry)
      exports: ["ui-auth-flow"]
    - path: src/widget/runtime/createOrRefreshEmbedFromUrl.ts
      provides: Integración PAT store y estados de auth en pipeline end-to-end
      exports: ["createOrRefreshEmbedFromUrl"]
  key_links:
    - from: src/widget/code.ts
      to: src/widget/runtime/patSessionStore.ts
      via: runtime inicializa store de PAT y lo inyecta al pipeline
      pattern: "patSessionStore|loadPatSessionStore|createPatSessionStore"
    - from: src/widget/code.ts
      to: src/widget/runtime/createOrRefreshEmbedFromUrl.ts
      via: create/refresh incluyen patStore y manejan auth-needed
      pattern: "createOrRefreshEmbedFromUrl\("
    - from: src/widget/code.ts
      to: src/widget/runtime/redactSensitive.ts
      via: mensajes de error/sync se sanitizan antes de UI notify
      pattern: "redactSensitive\("
    - from: src/widget/ui.html
      to: src/widget/bridge/messages.ts
      via: UI envía comandos submit/forget PAT y consume estado auth
      pattern: "submit-pat|forget-pat|runtime-status"
---

<objective>
Implementar el flujo privado con PAT por fichero en el widget, incluyendo reintentos manuales, errores diferenciados y garantías de no exposición de secretos.

Purpose: Cumplir SEC-01/SEC-02/SEC-03 con integración real runtime/UI y hardening de seguridad.
Output: Store de PAT seguro + UX inline de auth + pipeline con estados de error diferenciados + tests de no filtrado.
</objective>

<execution_context>
@/Users/cesar/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cesar/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-private-access-pat-safety/07-CONTEXT.md
@.planning/phases/07-private-access-pat-safety/07-RESEARCH.md
@src/core/auth/patStore.ts
@src/core/github/readGithubFileWithAuth.ts
@src/core/github/authUxMessages.ts
@src/widget/runtime/createOrRefreshEmbedFromUrl.ts
@src/widget/code.ts
@src/widget/ui.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar store seguro de PAT por fichero y utilidades de redacción</name>
  <files>src/widget/runtime/patSessionStore.ts, src/widget/runtime/redactSensitive.ts, src/widget/__tests__/securityRedaction.test.ts</files>
  <action>Crear un store de sesión para PAT keyed por `sourceKey`, con cache en memoria y persistencia en `clientStorage` cifrado (fallback seguro si crypto/storage no está disponible). Implementar marcado de estado `valid/invalid` y motivo (`expired_pat`, `current_pat`). Añadir utilitario de redacción para evitar exposición de tokens en `message/details/logs`. Cubrir con tests de persistencia, invalidación y redacción.</action>
  <verify>npm test -- src/widget/__tests__/securityRedaction.test.ts && npm run typecheck</verify>
  <done>Existe infraestructura segura para PAT y sanitización de secretos reutilizable por runtime/UI (SEC-03 base).</done>
</task>

<task type="auto">
  <name>Task 2: Extender pipeline/runtime para auth privada con reintento manual</name>
  <files>src/widget/runtime/createOrRefreshEmbedFromUrl.ts, src/widget/code.ts, src/widget/bridge/messages.ts, src/widget/bridge/parseUiCommand.ts</files>
  <action>Integrar `patSessionStore` en el pipeline para que `readGithubFileWithAuth` use PAT por `sourceKey`, marque inválidos diferenciados y devuelva estado de auth para UI. Extender bridge con comandos `submit-pat` y `forget-pat` vinculados a `sourceKey`. En runtime, al recibir `MISSING_PAT`, `EXPIRED_PAT` o `CURRENT_PAT`, mantener embed existente, publicar estado auth requerido y permitir reintento manual tras actualizar token.</action>
  <verify>npm test -- src/widget/__tests__/runtimePipeline.test.ts && npm test -- src/widget/__tests__/widgetStatePersistence.test.ts && npm run typecheck</verify>
  <done>Flujo privado funciona end-to-end sin reinicio de instancia y con estados auth diferenciados (SEC-01, SEC-02).</done>
</task>

<task type="auto">
  <name>Task 3: Implementar UX inline PAT y pruebas de no filtrado</name>
  <files>src/widget/ui.html, src/widget/code.ts, src/widget/__tests__/patAccessFlow.test.ts, src/widget/code.js</files>
  <action>Actualizar UI para mostrar prompt inline de PAT solo en errores auth, con acciones `Guardar PAT`, `Reintentar`, `Olvidar PAT de este fichero`. Incluir copy final acordado para missing/expired/current PAT y desactivar auto-retry. Añadir tests de flujo privado (missing->submit->retry->success, expired/current->replace PAT) y aserciones de seguridad que verifiquen ausencia de token en embed metadata/snapshot y payload visual. Recompilar bundle `src/widget/code.js`.</action>
  <verify>npm test -- src/widget/__tests__/patAccessFlow.test.ts && npm run widget:build && npm run widget:check && npm run typecheck</verify>
  <done>UX de auth privada queda operativa y el token permanece fuera de canvas/estado visible (SEC-01, SEC-02, SEC-03).</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- src/widget/__tests__/securityRedaction.test.ts` pasa en verde
- [ ] `npm test -- src/widget/__tests__/patAccessFlow.test.ts` pasa en verde
- [ ] `npm test -- src/widget/__tests__/runtimePipeline.test.ts` pasa en verde
- [ ] `npm test -- src/widget/__tests__/widgetStatePersistence.test.ts` pasa en verde
- [ ] `npm run widget:build` pasa en verde
- [ ] `npm run widget:check` pasa en verde
- [ ] `npm run typecheck` pasa sin errores
- [ ] SEC-01, SEC-02 y SEC-03 quedan cubiertos con evidencia explícita en summary
</verification>

<success_criteria>

- Todas las tareas completadas
- Flujo privado solicita PAT en el momento correcto y permite reintento manual exitoso
- Mensajes de PAT ausente/expirado/scope insuficiente aparecen diferenciados
- No hay serialización o exposición del PAT en canvas, metadata, snapshots ni eventos visuales
  </success_criteria>

<output>
After completion, create `.planning/phases/07-private-access-pat-safety/07-01-SUMMARY.md`
</output>
