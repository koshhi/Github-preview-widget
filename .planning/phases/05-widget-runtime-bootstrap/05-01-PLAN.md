---
phase: 05-widget-runtime-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - manifest.json
  - package.json
  - src/widget/code.ts
  - src/widget/ui.html
  - src/widget/bridge/messages.ts
  - src/widget/bridge/parseUiCommand.ts
  - src/widget/bootstrap/createSeedEmbed.ts
  - src/widget/bootstrap/widgetMetadata.ts
  - src/widget/__tests__/bridge.test.ts
  - src/widget/__tests__/bootstrap.test.ts
  - .planning/STATE.md
autonomous: true
requirements:
  - WDG-01
  - WDG-02
  - QLT-04
user_setup: []
must_haves:
  truths:
    - "El proyecto incorpora runtime base de Widget API con manifest válido y estructura de archivos explícita."
    - "La UI mínima permite pegar URL y enviar comando `create-preview` por un bridge tipado."
    - "El runtime crea una instancia semilla con metadata mínima (`sourceKey`, `lastSync`, `syncState`)."
    - "Se añaden scripts de build/check del widget sin romper `npm test` ni `npm run typecheck`."
  artifacts:
    - path: manifest.json
      provides: Configuración de carga del widget en Figma Dev Mode
      exports: ["widgetApi", "main", "ui"]
    - path: src/widget/bridge/messages.ts
      provides: Contratos tipados de mensajes entre UI y runtime
      exports: ["UiToWidgetCommand", "WidgetToUiEvent"]
    - path: src/widget/bootstrap/createSeedEmbed.ts
      provides: Creación de embed semilla para fase de bootstrap
      exports: ["createSeedEmbed"]
    - path: src/widget/bootstrap/widgetMetadata.ts
      provides: Inicialización de metadata persistente mínima por instancia
      exports: ["createSeedMetadata"]
    - path: package.json
      provides: Scripts de build/check del widget
      exports: ["widget:build", "widget:check"]
  key_links:
    - from: src/widget/ui.html
      to: src/widget/bridge/messages.ts
      via: UI emite comandos que respetan contrato tipado
      pattern: "create-preview|refresh-preview"
    - from: src/widget/code.ts
      to: src/widget/bridge/parseUiCommand.ts
      via: Runtime parsea y valida comandos antes de actuar
      pattern: "parseUiCommand\("
    - from: src/widget/code.ts
      to: src/widget/bootstrap/createSeedEmbed.ts
      via: Comando de create dispara composición de instancia semilla
      pattern: "createSeedEmbed\("
    - from: src/widget/bootstrap/createSeedEmbed.ts
      to: src/widget/bootstrap/widgetMetadata.ts
      via: Metadata mínima se construye de forma centralizada
      pattern: "createSeedMetadata\("
---

<objective>
Implementar el bootstrap de runtime para un widget real de Figma, con UI mínima de intake y bridge de mensajes tipado.

Purpose: Cumplir WDG-01/WDG-02/QLT-04 y dejar base estable para integración end-to-end en fase 6.
Output: Runtime shell de widget + UI bridge + creación de embed semilla + scripts de build/check + tests del bootstrap.
</objective>

<execution_context>
@/Users/cesar/.codex/get-shit-done/workflows/execute-plan.md
@/Users/cesar/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-widget-runtime-bootstrap/05-CONTEXT.md
@.planning/phases/05-widget-runtime-bootstrap/05-RESEARCH.md
@src/core/url/ingestGithubFileUrl.ts
@src/core/canvas/composeEmbedBlock.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold de runtime Widget API y scripts de desarrollo</name>
  <files>manifest.json, package.json, src/widget/code.ts, src/widget/ui.html</files>
  <action>Crear el scaffold base del widget (manifest + entrypoint runtime + UI mínima) con estructura explícita en `src/widget/`. Añadir scripts `widget:build` y `widget:check` en `package.json` sin alterar comportamiento de `npm test` y `npm run typecheck`. Incluir un render inicial seguro para validar carga en Dev Mode.</action>
  <verify>npm run widget:build && npm run widget:check && npm run typecheck</verify>
  <done>El widget carga localmente con manifest válido y existen comandos de build/check reproducibles (WDG-01, QLT-04).</done>
</task>

<task type="auto">
  <name>Task 2: Implementar bridge tipado UI ↔ runtime para create-preview</name>
  <files>src/widget/bridge/messages.ts, src/widget/bridge/parseUiCommand.ts, src/widget/ui.html, src/widget/code.ts, src/widget/__tests__/bridge.test.ts</files>
  <action>Definir contratos de mensajes (`create-preview`, `refresh-preview` placeholder) y parser/guard de comandos recibidos por runtime. Implementar UI mínima con campo URL + botón principal, enviando comando tipado al runtime. Añadir tests para validar parsing, rechazo de payload inválido y contrato estable del bridge.</action>
  <verify>npm test -- src/widget/__tests__/bridge.test.ts && npm run typecheck</verify>
  <done>La UI puede enviar un comando válido de create-preview y el runtime lo interpreta de forma segura y testeada (WDG-02).</done>
</task>

<task type="auto">
  <name>Task 3: Crear instancia semilla y metadata mínima del embed</name>
  <files>src/widget/bootstrap/widgetMetadata.ts, src/widget/bootstrap/createSeedEmbed.ts, src/widget/code.ts, src/widget/__tests__/bootstrap.test.ts</files>
  <action>Implementar helpers para metadata semilla (`sourceKey`, `lastSync`, `syncState`) y composición inicial de embed en canvas con estructura base. Conectar el handler `create-preview` para crear la instancia semilla sin flujo completo de render/auth (fase 6/7). Añadir tests de metadata defaults y forma del embed inicial.</action>
  <verify>npm test -- src/widget/__tests__/bootstrap.test.ts && npm run typecheck</verify>
  <done>El comando create-preview produce una instancia semilla con metadata mínima consistente para siguientes fases (WDG-01 base de ejecución).</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run widget:build` pasa en verde
- [ ] `npm run widget:check` pasa en verde
- [ ] `npm test -- src/widget/__tests__/bridge.test.ts` pasa en verde
- [ ] `npm test -- src/widget/__tests__/bootstrap.test.ts` pasa en verde
- [ ] `npm run typecheck` pasa sin errores
- [ ] WDG-01, WDG-02 y QLT-04 están cubiertos explícitamente por tareas y checks
</verification>

<success_criteria>

- Todas las tareas completadas
- Runtime widget cargable con manifest válido
- UI mínima funcional para intake de URL y bridge estable
- Scripts de build/check añadidos sin romper flujos actuales de test/typecheck
  </success_criteria>

<output>
After completion, create `.planning/phases/05-widget-runtime-bootstrap/05-01-SUMMARY.md`
</output>
